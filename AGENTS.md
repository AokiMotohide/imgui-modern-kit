# AGENTS.md

このファイルは、このリポジトリで Codex が作業するときの必須規則を定める。規則を満たすことだけを理由に、依頼外の文書更新、全面調査、全体ビルド、GUI確認、配布作業へ範囲を広げない。

## プロジェクト

`imgui-modern-kit` は C++20 と Dear ImGui を使用する静的UIライブラリである。主要な公開ヘッダーは `include/imkit/`、実装は `src/`、テストは `tests/`、開発用Galleryは `examples/gallery/` にある。

ライブラリはDear ImGuiのContext、backend、font、renderer、永続化、workerを所有しない。これらは利用側ホストが所有する。公開APIと所有権の正本は `docs/architecture.md`、検証範囲は `docs/validation.md` を参照する。

## CodeGraph

- コードの所在、呼出し経路、依存範囲を調べるときは、リポジトリ直下の `.codegraph/` を使い、`rg`、ファイル列挙、個別ファイルの通読より先に `codegraph explore "<質問またはシンボル>"` を実行する。
- 作業開始時に `codegraph status` で索引状態を確認する。対象コードに未反映の変更があれば、調査前に `codegraph sync` を実行する。
- CodeGraphで扱えないCMake、Markdown、生成物、画像、設定値だけを、対象を限定して直接調査する。
- コード変更後は `codegraph sync` を実行し、索引の更新成功を確認する。`.codegraph/codegraph.db` など `.codegraph/.gitignore` が除外するローカルDBはコミットしない。

## 機密保持とリポジトリ分離

- このリポジトリの作業、回答、文書、コミット、Issue、Pull Request、Release、ログ、生成物に、ユーザーが現在の依頼で明示的に開示を指示していない他リポジトリの情報を一切含めない。
- 他リポジトリ情報の開示は、ユーザーが対象情報と開示先を現在の依頼で個別に指定した場合だけ許可される。過去の許可、包括的な作業依頼、読み取り権限、ローカルで参照できることを開示許可とみなさない。
- 開示禁止の情報には、他リポジトリの名称、パス、存在、開発状況、計画、タスク、ブランチ、コミット、差分、dirty状態、設計、未公開機能、障害、検証結果、配布状況、担当者情報を含む。断片的な言及、比較から推測できる表現、要約、伏字による示唆も禁止する。
- Codexのmemory、過去会話、別worktree、ローカルファイル、外部サービス、他セッションから得た他リポジトリ情報を、このリポジトリの前提、根拠、例、比較対象として使用しない。質問されても、現在の依頼で開示許可がなければ回答しない。
- 他リポジトリを読み取り、検索、変更、ビルド、テスト、実行しない。現在の依頼でユーザーが対象と目的を明示した場合だけ、その依頼に必要な最小範囲で扱う。その場合も、明示指示がない限り得た情報をこのリポジトリのファイルや外部公開物へ転記しない。
- 他リポジトリ由来の運用知識は、出所、固有名詞、状況、数値、設計情報を完全に除いた一般規則としてのみ採用できる。機密保持と成果の説明が衝突する場合は、他リポジトリ情報を省略し、このリポジトリ内で確認できた事実だけを報告する。

## 作業範囲

- 作業開始時とコミット直前に、現在のブランチ、`git status --short`、対象差分を確認する。
- 他セッションまたはユーザーの変更を編集、整形、削除、ステージ、コミットしない。所有範囲が不明な差分は保持する。
- 依頼範囲を越える大規模なリファクタリング、公開API変更、ABI変更、依存追加、対応Dear ImGui版変更、CMake・install・package構成変更は、実装前にユーザーの確認を得る。
- 調査、監査、計画、レビュー、説明の依頼ではコードを変更しない。実装依頼では、必要な実装と直接関係する検証まで進める。
- モデルと推論強度はユーザーが指定する。このファイルでは固定しない。

## 実装境界

- 公開APIは `include/imkit/`、実装は `src/` に分離し、内部実装を公開ヘッダーへ漏らさない。
- Dear ImGuiの標準overload、戻り値、default引数、ID、focus、callback、disabled、Begin/End、clipping、入力編集の契約を保つ。対応版を増やす場合はversion guardを緩めるだけで完了としない。
- Context、font、texture、renderer、backend、theme状態、animation状態の寿命と所有権はホスト側に残す。グローバルregistry、暗黙のContext生成、backend初期化、ホスト固有サービスをライブラリへ導入しない。
- `FontSet` などの非所有参照を所有参照として扱わない。scopeは対象Contextより前に破棄する。
- 新しい部品は、意味token、Dear ImGui公開widget、公開DrawListの順で構成する。入力系widgetの挙動を独自実装で置き換えない。
- 公開APIを追加または変更した場合は、該当するAPI compile fixture、consumer test、英語・日本語の公開文書、API inventoryへの影響を確認する。更新不要と判断した項目は理由を報告する。

## ビルドとテスト

Windowsの通常確認は、構成済みの `build/windows-debug/` を使うDebug増分ビルドとし、変更に必要な最小ターゲットと関連テストだけを実行する。

```powershell
cmake --build --preset windows-debug --target imkit --parallel
cmake --build --preset windows-debug --target <対象テストまたはimkit_gallery> --parallel
ctest --test-dir build/windows-debug -C Debug -R "<対象テスト>" --output-on-failure
```

- 構成済みbuildがある通常作業では `cmake --preset windows-debug` を繰り返さない。CMake入力、依存定義、ツールチェーンの変更、新規build、自動再構成失敗、構成不整合がある場合だけ再構成する。
- ライブラリ変更ではまず `imkit`、テスト変更では該当テストtarget、Gallery変更では `imkit_gallery` を個別にビルドする。通常確認で `ALL_BUILD`、`clean`、`Rebuild` は使わない。
- Release、install、package、SDK consumer、全テストは、配布・互換性・公開API・CMake契約へ変更が及ぶ場合、ユーザーが指定した場合、または限定確認では検出できない場合だけ実行する。
- ビルド前に実行中のCMake、MSBuild、コンパイラとコマンドラインを一度確認する。同じbuildディレクトリまたは同じ出力へ書き込む処理だけを競合とし、別buildや別セッション全体の完了は待たない。
- 競合を避けるため専用buildを作る場合は出力先を分離する。削除できるのは自分が作成し、使用中でないことを確認した専用buildだけである。共有buildや所有者不明の生成物は削除しない。
- 長時間処理は固定時間のsleepで待たず、プロセスと出力更新を追跡する。ツールの待機タイムアウトだけを失敗と扱わない。
- 最小ビルドまたは対象テストが1回成功した時点で、その確認は完了とする。性能計測または不安定な再現の必要がない限り、no-op再ビルド、同一target再実行、念押しの全体確認を追加しない。
- compile/link、公開IOによる操作、GPU backbuffer画像、native OS/IME、他アプリ統合、配布物検証を区別する。実施していない確認を合格として報告しない。

## 文書と生成物

- コード変更に伴う文書更新は、公開API、導入方法、対応環境、所有権、配布契約、ユーザー可視動作が変わる場合だけ行う。無関係な文書を一括更新しない。
- 英語と日本語の対になる公開文書は、意味と制約を一致させる。
- `docs/images/` の画像は、実際のnative Galleryを描画・captureしたものだけを使用する。HTML mockや合成画像を実装済みUIの証拠として扱わない。
- `out/`、build出力、検証ログ、capture一式など再生成可能な成果物は、明示依頼がない限りコミットしない。
- icon、font、画像、第三者コードを追加・更新する場合は、provenance、license、`THIRD_PARTY_NOTICES.md`、配布対象への影響を確認する。

## コミットと公開

- 実装またはリポジトリ内ルール変更が完了し、必要な検証に合格した場合は、ユーザーがコミット不要と明示しない限り日本語のメッセージでコミットする。
- 対象ファイルだけをステージする。コミット前に `git status --short`、ステージ済み差分、`git diff --cached --check` を確認する。
- 検証不能な未完了状態、所有範囲が不明な差分、他セッションの変更はコミットしない。
- push、Pull Request、Issue、Release、外部メッセージ送信は、ユーザーが明示した場合だけ実行する。
- 公開前に「機密保持とリポジトリ分離」を再確認し、他リポジトリ情報が含まれていないことを確認する。
