# GitHub profile proposal / GitHub表示案

Repository settings are not changed by the source tree. The maintainer can apply the following stable metadata when publication is requested.

リポジトリ設定はsource treeの変更対象外です。公開作業を行う場合は、次の安定したmetadataを使用できます。

## Description

> Modern C++20 design system, native controls, themes, icons and editor UI building blocks for Dear ImGui.

## Topics

`dear-imgui`, `imgui`, `cpp20`, `gui`, `ui-components`, `design-system`, `immediate-mode-gui`, `developer-tools`, `editor-ui`, `cmake`

Avoid numeric feature counts and evolving Editor Suite claims in repository metadata.

機能数や更新中のEditor Suite詳細はmetadataへ固定しません。
