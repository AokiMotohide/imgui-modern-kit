# 設定画面の作成

[English](build-settings-screen.md) · [目次](../目次.md) · [仕組みと設計思想](../getting-started/仕組みと設計思想.md)

本ドキュメントでは、ImKit のコンポーネントを組み合わせて、実践的な設定画面（設定フォーム）を構築する手順を解説します。
ImKit における設定画面の実装パターンは非常にシンプルで、**「ホスト側が所有する構造体（データモデル）」** と、**「それを表示・編集する設定行コントロール」** で構成されます。

![テーマ付きパネルを持つ Gallery 概要](../images/gallery-overview.gif)

---

## 基本設計パターン

保守性の高い設定画面を作るための重要なルールです：

1. **状態はホスト側の構造体で一元管理する**: すべての設定値はアプリの struct に保持します。コントロールはそれを読み書きするだけで、ImKit は自ら設定を永続化（ファイル保存等）しません。
2. **1行につき1コントロール**: `BeginSettingRow` を使い、左側にラベル、右側にコントロールを整然と配置します。
3. **ImKit は保存要求（Request）を返すだけ**: ユーザーが「保存」ボタンを押した際、ImKit はイベントを報告するのみです。実際のバリデーションやファイル書き込み、通知の表示はホスト側で行います。

---

## 前提条件

1. Dear ImGui コンテキストとウィンドウ（あなたのアプリまたは Gallery）。
2. パネルに適用するための `Theme` オブジェクト。
3. CMake で `imkit::imkit` ターゲットをリンクした環境。

---

## ホスト側の状態構造体

すべての設定項目を1つの構造体にまとめます。これが唯一の信頼できるデータソース（Single Source of Truth）となります：

```cpp
#include <imkit/imkit.h>

struct DisplaySettings {
    bool isEnabled = true;               // 有効/無効トグル
    int qualityLevel = 1;                // 描画品質 (0: Low, 1: Medium, 2: High)
    char qualitySearchBuffer[64]{};      // コンボボックスの検索バッファ
    float exposureEV = 0.0f;             // 露出設定 (スライダー)
    char displayName[128] = "Main Display"; // 表示名 (テキスト入力)
    bool isSavedToastVisible = false;    // 保存完了通知の表示フラグ
};
```

---

## 設定行パターン

`BeginSettingRow` は、左列にラベル、右列に編集コントロールを配置する2列レイアウトを提供します：

```cpp
// 1. トグルスイッチ（単独で完結するため行で囲む必要はありません）
imkit::Toggle("ディスプレイを有効化", &settings.isEnabled, {&theme});

// 2. ラベル付きコントロール（BeginSettingRow で囲む）
if (imkit::BeginSettingRow("exposure_row", "露出補正")) {
    // 単位（EV）付きのドラッグ入力欄
    imkit::DragFloatWithUnit("##exposure", &settings.exposureEV, "EV", 0.01f, -8.0f, 8.0f);
    imkit::EndSettingRow(); // ※ BeginSettingRow が true を返した時のみ呼び出す
}
```

> [!TIP]
> コントロールの ID 文字列に `##` プレフィックス（例: `"##exposure"`）を付けることで、ラベルを表示せずに一意の ImGui ID を保持できます。

---

## 完全な設定画面コード

設定構造体の編集、バリデーション、および保存通知を含む完全な描画関数です：

```cpp
void RenderSettingsPanel(DisplaySettings& settings, const imkit::Theme& theme) {
    if (imkit::Begin("ディスプレイ設定", nullptr, ImGuiWindowFlags_AlwaysAutoResize)) {
        // 有効/無効のトグル
        imkit::Toggle("ディスプレイ出力を有効化", &settings.isEnabled, {&theme});
        ImGui::Separator();

        // 無効時は以降のコントロールをグレーアウト
        if (!settings.isEnabled) {
            ImGui::BeginDisabled();
        }

        // 表示名のテキスト入力
        if (imkit::BeginSettingRow("name_row", "表示デバイス名")) {
            ImGui::InputText("##device_name", settings.displayName, sizeof(settings.displayName));
            imkit::EndSettingRow();
        }

        // 露出の数値入力（単位付き）
        if (imkit::BeginSettingRow("exposure_row", "露出補正")) {
            imkit::DragFloatWithUnit("##exposure", &settings.exposureEV, "EV", 0.05f, -4.0f, 4.0f);
            imkit::EndSettingRow();
        }

        // 描画品質の選択（検索付きコンボボックス）
        if (imkit::BeginSettingRow("quality_row", "レンダリング品質")) {
            const char* qualityOptions[] = {"低 (Low) - 30fps", "中 (Medium) - 60fps", "高 (High) - 120fps"};
            imkit::SearchableCombo("##quality", &settings.qualityLevel, qualityOptions, 3,
                                   settings.qualitySearchBuffer, sizeof(settings.qualitySearchBuffer));
            imkit::EndSettingRow();
        }

        if (!settings.isEnabled) {
            ImGui::EndDisabled();
        }

        ImGui::Spacing();
        ImGui::Separator();
        ImGui::Spacing();

        // 保存ボタン
        if (imkit::ActionButton("設定を保存", imkit::ActionVariant::Primary)) {
            SaveSettingsToDisk(settings);
            settings.isSavedToastVisible = true;
        }

        // 保存完了トースト通知
        if (settings.isSavedToastVisible) {
            imkit::NotificationView notify{
                "設定を保存しました",
                "新しいディスプレイ構成が正常に適用されました。",
                imkit::FeedbackKind::Success
            };
            if (imkit::NotificationCard("save_notify", notify)) {
                settings.isSavedToastVisible = false; // 閉じるボタン押下で非表示
            }
        }
    }
    imkit::End();
}
```

---

## 保存・バリデーション・通知

- **バリデーション**: 入力値が不正な場合は、`imkit::ValidationMessage("エラー内容")` をコントロールの直後に呼び出すことで、枠線の赤色ハイライトとツールチップ表示が行われます。
- **通知**: `imkit::NotificationCard` は、ホスト側のフラグによって表示を制御し、ユーザーが「×」ボタンを押したときに閉じる要求（真）を返します。自動消滅タイマーなどもホスト側の時間管理ロジックで制御します。

---

## 実行と動作確認

ImKit Gallery を起動して **Components: Basic** または **Settings** ページを開くと、これらの設定行コントロールの実動デモを確認できます：

```powershell
cmake --build --preset windows-debug --target imkit_gallery --parallel
./build/windows-debug/catalog/Debug/imkit_gallery.exe
```

---

## 内部動作のまとめ

- `BeginSettingRow` は Dear ImGui の `Table` API を内部で使用し、2列の均等な余白配置を自動計算します。
- `EndSettingRow` は必ず `BeginSettingRow` が `true` を返したときだけ呼び出します。
- すべてのコントロールはホストの変数アドレスを直接参照して描画・更新します。

---

## クリーンな設計を保つための規則

- **設定データは1つの struct にまとめる**: グローバル変数や複数のバラバラな引数を避け、設定構造体を渡すようにします。
- **ImKit に永続化を期待しない**: 設定ファイルの保存先パスや JSON/YAML のシリアライズはアプリ側で行います。
- **Begin/End のペアを厳守する**: スコープ管理を徹底することで、レイアウト崩れを防ぎます。

---

## 次に読むべきチュートリアル

- **[基本コンポーネント](../components/基本コンポーネント.md)** — ボタン、バッジ、セグメントなどの全コントロール仕様。
- **[ノードエディタの作成](ノードエディタの作成.md)** — スナップショット/リクエスト方式によるグラフエディタの構築。
- **[タイムラインの作成](タイムラインの作成.md)** — 動画やアニメーション向けのマルチトラックタイムライン構築。
